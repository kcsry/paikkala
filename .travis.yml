sudo: false
language: python
cache:
  directories:
    - "$HOME/.cache/pip"
env:
- DEBUG=1
python:
  - "3.6"
install:
  - pip install -U pip
  - pip install -U tox-travis tox
script:
  - tox
after_success:
  - bash <(curl -s https://codecov.io/bash)
